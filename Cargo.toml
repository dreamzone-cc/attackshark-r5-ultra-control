[package]
name = "glitch-r5u"
version = "1.1.0"
edition = "2021"
authors = ["dreamzone-cc"]
description = "Glitch R5U: Native Linux Control Suite for Attack Shark R5 Ultra Gaming Mouse (Rust + Slint UI)"
license = "MIT"

[dependencies]
slint = "1.17.1"
i-slint-backend-winit = "1.17.1"
ksni = { version = "0.3.6", default-features = false, features = ["blocking"] }
libc = "0.2.189"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
image = { version = "0.25.10", default-features = false, features = ["png"] }

[build-dependencies]
slint-build = "1.17.1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
